WITH Ada.Text_IO, Ada.Integer_Text_IO, duracion_tarea;
USE Ada.Text_IO, Ada.Integer_Text_IO;

PROCEDURE main IS
   h, m, s: Natural;
   duracion: Positive;
BEGIN
   get(h);
   get(m);
   get(s);
   get(duracion);
   duracion_tarea(h, m, s, 3600);
   put(h, m, s, dia_siguiente);

END main;