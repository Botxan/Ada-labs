PROCEDURE dia_anterior(dd, mm, aa: in out Positive) IS
   es_bisiesto: Integer range 0..1 := if (yy rem 2 = 0) and ((yy rem 100 /= 0) or (yy rem 400 = 0)) then 1 else 0; -- Comprobar si es bisiesto

   -- Formula para calcular dias de cada mes, idea tomada de:
   -- http://www.dispersiondesign.com/articles/time/number_of_days_in_a_month
   dias_en_mm: Integer = if (31 - (m = 2) then 3 - es_bisiesto else (m - 1) rem 7 rem 2;
BEGIN

   if (dd = 0) then
      dd = 31;
      if (mm = 0) then
         mm = 12;
         ir (yy = )
      else
         mm = mm - 1;
   else
      dd = dd - 1;
   end if;

END dia_anterior;