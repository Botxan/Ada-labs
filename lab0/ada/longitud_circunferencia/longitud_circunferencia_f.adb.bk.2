WITH Ada.Numerics; Use Ada.Numerics;
FUNCTION longitud_circunferencia_f(r: in Positive) RETURN Float IS
   -- Pre: no hace falta indicar precondición dado que el parametro r es de tipo Positive
   -- Post: return 2 * Pi * r
BEGIN
    return 2.0 * Pi * Float(r);
END longitud_circunferencia_f;
