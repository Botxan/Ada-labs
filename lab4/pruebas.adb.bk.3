with ada.Text_IO, ada.Integer_Text_IO, Laboratorio04;
use ada.Text_IO, ada.Integer_Text_IO, Laboratorio04;

procedure pruebas is
   eslo: Boolean;

   omirp, posicion: Natural;
   num: Natural;
begin

   --pos_omirp(omirp, posicion);
   --put(omirp);
   --put(posicion);

   -- lleva_a_kaprekar;

   es_collatz(eslo);
   es_collatz(eslo);
   es_collatz(eslo);
   es_collatz(eslo);
   es_collatz(eslo);


   --new_line(2);
   --put("Mete algun numerito");
   --skip_line;
   --get(num);
   --put(num);
end pruebas;