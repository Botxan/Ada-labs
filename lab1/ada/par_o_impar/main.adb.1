WITH Ada.Text_IO, Ada.Integer_Text_IO, par_o_impar;
USE Ada.Text_IO, Ada.Integer_Text_IO;

PROCEDURE main is
   num: Positive;
   paR_
BEGIN
   put("Introduce un numero: ");
   get(num);

   new_line;

if (

   put("El numero es par: " & Boolean'Image(par_o_impar(num)));

END main;