PROCEDURE ordenar_dos_numeros_1 (num1, num2: in out Positive) IS
   -- Pre: num1 y num2 > 0. Esto siempre sera asi puestoque lo shemos tipado como Positive
   -- Post: num1 sera el menor y num2 el mayor. Si son iguales, se deja los valores por defecto.
   temp_num: Positive; -- variable auxiliar para intercambiar los valores de num1 y num2
BEGIN
   if (num1 > num2) then
      temp_num := num1;
      num1 := num2;
      num2 := temp_num;
   end if;
END ordenar_dos_numeros_1;